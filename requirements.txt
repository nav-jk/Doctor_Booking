# Core Dependencies
Django>=5.0,<5.3
gunicorn  # Required for deployment

# Database (Use only one, depending on your choice)
psycopg2-binary  # PostgreSQL (for production)
# mysqlclient  # Uncomment if using MySQL
# sqlite3 is built-in, no need to install separately

# Static Files Management (for Render & production)
whitenoise

# Environment Variables
python-dotenv

# PDF Generation
reportlab

# For Async Websockets (If using Django Channels)
daphne  # Required for WebSockets if real-time updates are needed

# Optional: If using Django REST Framework for APIs
# djangorestframework  

# Optional: If using Celery for background tasks
# celery  

# Frontend Dependencies (If using Tailwind or Bootstrap with Django)
# django-tailwind  

# Miscellaneous
pillow  # Required for image uploads
